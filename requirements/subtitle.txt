# WhisperX 字幕生成依賴
# 包含語音識別、強制對齊所需的核心套件

# WhisperX 核心
whisperx>=3.1.0

# PyTorch 核心 (支援 Apple Silicon MPS 加速)
torch>=2.0.0
torchaudio>=2.0.0

# Whisper 後端
faster-whisper>=0.10.0

# 語音處理工具
# (WhisperX 會自動安裝這些，但明確列出以確保版本相容)
