# 兩步驟播客生成工作流程

將播客生成拆分成兩個獨立步驟，讓你更好控制每個階段。

## 📋 工作流程

```
Step 1: Podcastfy 生成腳本 → 輸出到 ./scripts/
Step 2: Gemini TTS 生成音頻 → 輸出到 ./audio/
```

## 🚀 使用方法

### Step 1: 生成腳本

```bash
# 激活虛擬環境
source venv312/bin/activate

# 執行腳本生成（使用 podcast_config.yaml 設定）
python step1_generate_script.py
```

**輸出**：
- 腳本保存在 `./scripts/script_[時間戳]/podcast_script.txt`
- 包含字數統計和準確度分析
- 自動顯示下一步指令

### Step 2: 生成音頻

```bash
# 方法一：使用最新生成的腳本
python step2_generate_audio.py

# 方法二：指定特定腳本目錄
python step2_generate_audio.py ./scripts/script_20250820_130000
```

**輸出**：
- 音頻保存在 `./audio/audio_[時間戳]/podcast.wav`
- 使用 Multi-Speaker TTS（兩個不同聲線）
- 包含完整的元數據記錄

## 🎯 優點

1. **分段控制**：可以先檢查腳本品質，滿意後再生成音頻
2. **節省成本**：避免不滿意的腳本浪費 TTS API 調用
3. **靈活調整**：可以手動編輯腳本後再生成音頻
4. **問題隔離**：容易判斷是腳本問題還是 TTS 問題

## 📝 配置說明

在 `podcast_config.yaml` 中調整：

```yaml
basic:
  target_minutes: 1  # 目標長度（分鐘）
  english_level: "A2"  # 英語難度

voices:
  host_voice: "Kore"  # 主持人聲線
  expert_voice: "Aoede"  # 專家聲線

input:
  source: "你的內容來源"  # URL、文本或檔案路徑
```

## 🔍 檢查腳本品質

生成腳本後，Step 1 會顯示：
- **實際字數** vs **目標字數**
- **準確度百分比**（理想範圍：60-140%）

如果腳本太長或太短，可以：
1. 調整 `target_minutes` 參數
2. 修改 `podcast_config.yaml` 重新生成
3. 手動編輯腳本檔案

## 🎤 聲線選項

可用的 Gemini TTS 聲線：
- **Kore**: 溫和、親切的女聲
- **Puck**: 活潑、年輕的男聲
- **Charon**: 沈穩、權威的男聲
- **Fenrir**: 專業、清晰的男聲
- **Aoede**: 優雅、文藝的女聲

## ⚠️ 注意事項

1. **Python 版本**：需要 Python 3.11+ 
2. **套件版本**：google-genai >= 1.31.0（支援 MultiSpeakerVoiceConfig）
3. **API Key**：確保 `.env` 檔案有 `GEMINI_API_KEY`

## 🐛 問題排查

### 腳本生成問題
- 檢查 Podcastfy 是否正確安裝
- 確認輸入來源可訪問
- 查看 `./data/transcripts/` 目錄

### 音頻生成問題
- 確認 google-genai 版本：`pip show google-genai`
- 檢查腳本格式是否包含 `<Person1>` 和 `<Person2>` 標記
- 確認 API Key 有效且有配額

## 📊 輸出結構

```
podcastfy_test/
├── scripts/              # Step 1 輸出
│   └── script_[時間戳]/
│       ├── podcast_script.txt  # 生成的腳本
│       └── metadata.json       # 元數據
├── audio/                # Step 2 輸出
│   └── audio_[時間戳]/
│       ├── podcast.wav         # 音頻檔案
│       ├── script.txt          # 使用的腳本
│       └── metadata.json       # 完整元數據
```