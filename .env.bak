# =================================================================
# 播客生成系統 - API Keys 配置
# =================================================================
# 注意：所有應用設定已移至 podcast_config.yaml
# 這裡只保留敏感的 API Keys
# =================================================================

# Gemini API Key (必需)
# 用於：LLM 腳本生成 + Multi-Speaker TTS
GEMINI_API_KEY=AIzaSyBAZqPtrW7k3MWpvNQYFWnPERdVz5jCv0c

# OpenAI API Key (可選)
# 只有在 podcast_config.yaml 設定 use_podcastfy_tts: true 時需要
# OPENAI_API_KEY=

# ElevenLabs API Key (可選)
# 高品質語音替代方案
# ELEVENLABS_API_KEY=

# Google Cloud Storage 認證（用於 GCS 資料同步）
GOOGLE_APPLICATION_CREDENTIALS=/Users/chenliangyu/Desktop/podcast-workspace/storytelling-backend/secrets/google-translate-service-account.json
STORYTELLING_SYNC_BUCKET=gs://storytelling-output/output
# Limit CLI sync to audio/subtitle assets only
STORYTELLING_SYNC_EXCLUDE_REGEX="(^|/)\\.DS_Store$|(^|/)\\.gitignore$|(^|/)\\.env$|(^|/)\\.pytest_cache($|/.*)|^(?!.*\\.(mp3|srt)$).*"
